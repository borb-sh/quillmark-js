package-lock.json

# Hand-formatted canon and design prose and the reference-quill fixture tree:
# prettier would collapse aligned code-sketch comments and churn a test input.
#
# Prettier resolves its ignore files against the cwd, not the workspace root, so a run
# started inside a package reads that package's and never this one — which is how canon
# got reformatted once with nothing failing. Each package carries its own copy of the
# lines a cwd there can reach; what is left below is the root's alone.
**/prose/
fixtures/

# Claude Code tooling — agent config and skills copied from the sibling quillmark
# checkout (hand-formatted prose). Never shipped, not package source, so it stays
# out of the prettier gate like `prose/`.
.claude/

# Build output, in every package.
**/dist/
**/build/
**/.svelte-kit/

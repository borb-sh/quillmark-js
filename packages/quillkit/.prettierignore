# Prettier reads the ignore file at its cwd, not the workspace root's, so a run started
# here reads this one: canon, the build output, and the quiver packed into the served tree.
prose/
dist/
build/
.svelte-kit/
client/public/quiver/
